# Vote System (website)
## Installation

```bash
$ npm install
```

## Running the app

```bash
# development
$ npm run start

```
## Serve
### Build as docker (docker-compose)
```bash
# deploy to docker (docker-compose)
$ docker-compose -p "web" up -d
